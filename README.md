# AirBallot
AirBallot is a zero-backend, offline-first Electronic Voting Machine (EVM) built entirely in Vanilla JavaScript. It solves the classic web-sandbox paradox by combining the modern Web File System Access API with IndexedDB to treat a local text file as a persistent, real-time, read/write database—requiring no Node.js, no SQL, and zero cloud hosting.
